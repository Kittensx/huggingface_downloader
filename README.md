# huggingface_downloader
Download any Hugging Face repo (or subfolder) from a web URL into a local directory—supports private/gated models, subfolder-only sync, and version-agnostic auth. Includes a Windows PowerShell launcher.
